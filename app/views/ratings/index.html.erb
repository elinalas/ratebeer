<h2>List of ratings</h2>

<ul>
  <% @ratings.each do |rating| %>
      <li> <%= rating.to_s %> <%= link_to rating.user.username, rating.user%></li>
  <% end %>
</ul>


<% if current_user %>
    <%= link_to 'New Rating', new_rating_path %>
<% else %>
    You have to log in to make ratings.

<% end %>

