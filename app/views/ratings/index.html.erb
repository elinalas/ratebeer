<h2>List of ratings</h2>

<ul>
  <% @ratings.each do |rating| %>
      <li> <%= rating.to_s %> <%= link_to rating.user.username, rating.user%></li>
  <% end %>
</ul>

<% if @ratings.empty? %>
    Help us to rate beers!
<% else %>
    <%=pluralize( @ratings.size, 'ratings')%><br>
<% end %><br><br>

    <%= link_to 'New Rating', new_rating_path if current_user %>


