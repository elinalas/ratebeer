<p id="notice"><%= notice %></p>

<p>
  <h2><%= @brewery.name %></h2>

</p>

<p>
  <i>Eshtablished in <%= @brewery.year %></i>

</p>


<p>
  Number of beers:
  <%=@brewery.beers.count%>

</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <td><a href="<%=beer_path(beer.id) %>"><%=beer.name%></a></td>
  <% end %>
</p>

<% if @brewery.ratings.empty? %>
    Beers has not yet been rated!
<% else %>
    Beers has <%=pluralize( @brewery.ratings.size, 'rating')%> ratings<br>
    Average rating: <%=@brewery.average_rating%><br><br>
<% end %>


<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
